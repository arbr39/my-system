# Commands Sync Rule

> Применяется при добавлении новых slash-команд в бота

## Правило: Синхронизация списка команд

**При добавлении новой команды (`/command`) ОБЯЗАТЕЛЬНО обновить:**

1. **`src/handlers/start.py` → `COMMANDS_TEXT`**
   - Добавить команду в соответствующую секцию
   - Формат: `/command — Краткое описание`

2. **`CLAUDE.md` → таблица команд** (если есть)
   - Добавить команду в таблицу

## Структура COMMANDS_TEXT

```python
COMMANDS_TEXT = """
📖 *Все команды бота:*

*Основные:*
/start, /help, /commands

*Ежедневные:*
/today, /stats, /habits

*GTD:*
/inbox, /someday, /review

*Управление:*
/goals, /settings, /calendar

*Система наград:*
/rewards

*Принципы и даты:*
/principles, /dates

*Обратная связь:*
/report, /reports

*Автоматические напоминания:*
(список scheduler jobs)
"""
```

## Чеклист при добавлении команды

- [ ] Создан handler с `@router.message(Command("name"))`
- [ ] Router зарегистрирован в `bot.py` (перед inbox!)
- [ ] Команда добавлена в `COMMANDS_TEXT`
- [ ] Если есть callback кнопка — добавлена в `inline.py` главное меню
- [ ] Обновлён `CHANGELOG.md`

## Пример

```python
# При добавлении /newfeature:

# 1. В COMMANDS_TEXT добавить:
*Новая секция:*
/newfeature — Описание новой фичи

# 2. В bot.py:
from src.handlers import newfeature
dp.include_router(newfeature.router)  # перед inbox!
```
