# Reward System Rules (@whysasha)

> Применяется при работе с системой наград, геймификацией, мотивацией

## Принципы дофаминовых привязок

### 1. Награда ТОЛЬКО за результат
- Никакого "халявного" дофамина
- Измеримый результат обязателен
- Награда = эквивалент достижения

```python
# ✅ Правильно
if morning_completed:
    grant_reward(50, "Morning kaizen completed")

# ❌ Неправильно
if tried_morning or intended_morning:
    grant_reward(50, "Tried morning")
```

### 2. Больше результат = больше награда

```python
# Соразмерность наград
morning_kaizen = 50₽      # Базовый ритуал
task_done = 20₽           # Единичная задача
priority_task = +30₽      # Главное дело дня
weekly_review = 100₽     # Еженедельный большой ритуал
```

### 3. Награда сразу
Немедленное подкрепление после достижения.

```python
# ✅ Правильно
async def finish_morning():
    save_to_db()
    reward = grant_reward()  # Сразу!
    show_message(f"Отлично! +{reward}₽")

# ❌ Неправильно
async def finish_morning():
    save_to_db()
    # Награду дадим потом в конце дня...
```

### 4. Личный список наград
Пользователь сам определяет, что ему доставляет удовольствие.

```python
# Не хардкодить награды!
# Вместо этого - user-defined reward list
user.add_reward("Кофе", 150)
user.add_reward("Ресторан", 800)
```

---

## Anti-Cortisol Messaging

### ✅ DO: Празднуем победы
```
"Отлично! +50₽ за утренний кайдзен!"
"Заработано сегодня: 120₽"
"Ты можешь позволить себе кофе!"
"Главная задача выполнена! +30₽ бонус"
```

### ❌ DON'T: Стыдим за пропуски
```
"Ты пропустил спорт"
"Мог бы заработать больше"
"Streak потерян, начинай сначала"
"Опять ничего не сделал?"
```

### Правило: Нейтральность вместо боли
Пропустил действие = просто не получил награду (нейтрально), а не наказание (кортизол).

```python
# ✅ Правильно
if not exercised:
    # Просто не начисляем 30₽, не упоминаем вообще
    pass

# ❌ Неправильно
if not exercised:
    show_message("❌ Спорт пропущен, ты потерял 30₽")
```

---

## Штрафы (penalties)

Использовать ТОЛЬКО в последнюю очередь, когда:
1. Убраны все кортизоловые закрепления
2. Созданы сильные дофаминовые привязки
3. Пользователь **явно попросил** включить штрафы

```python
# По умолчанию выключено
fund.penalties_enabled = False

# Включается только вручную пользователем
```

---

## Database Design

### Модели наград:
- **RewardFund** — баланс, ставки, настройки (1 на user)
- **RewardTransaction** — история (N на fund)
- **RewardItem** — список наград (N на fund)

### Relationship pattern:
```python
User → RewardFund (1:1) → RewardTransaction (1:N)
                        → RewardItem (1:N)
```

---

## UI/UX Guidelines

### Balance Display
Показывать баланс часто и заметно:
- В главном меню наград: `💰 Баланс: 1,240₽`
- После каждого заработка: `📊 Баланс: 1,290₽`
- При просмотре награды: `✅ Можешь позволить!` или `❌ Не хватает 150₽`

### Transaction Breakdown
Детализация важна — пользователь должен видеть ЗА ЧТО заработал:

```
💰 Заработано сегодня:
• Рефлексия: +50₽
• Задачи (2): +40₽
• Главная задача: +30₽
• Спорт: +30₽

📊 Итого: +150₽ | Баланс: 1,440₽
```

### Spending Flow
1. Показать награду + цену + баланс
2. "Ты заслужил это!" messaging
3. Подтверждение траты
4. Celebration после: "Наслаждайся — ты это заслужил!"

---

## References

- Методичка @whysasha: `/Users/anonim/Desktop/Telegram/Методичка по мотивации BY WHYSASHA.pdf`
- Подробная версия: `/Users/anonim/Desktop/Telegram/whysasha (2) (2).pdf`
- GTD книга: David Allen "Getting Things Done"
